apiVersion: v1
kind: Service
metadata:
  name: rust-basic-api
  labels:
    app: rust-basic-api
spec:
  type: ClusterIP
  selector:
    app: rust-basic-api
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  sessionAffinity: None
